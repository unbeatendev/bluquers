{
  "name": "bluquers",
  "version": "1.0.0",
  "description": "Bluquers consultancy website",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf ./development && rm -rf ./.cache && parcel src/index.html --out-dir development -p 5501",
    "build": "parcel build src/index.html --out-dir dist --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.10"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "jquery": "^3.4.1"
  }
}
